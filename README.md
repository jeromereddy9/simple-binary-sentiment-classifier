# simple-binary-sentiment-classifier
A comparison between two implementations of a simple binary sentiment classifier. The first classifier is a custom implementation that explores using the embedded vocabulary as the learned representation and the second is a traditional binary classifier. 
